# 附件2-3完整 V2 模型设置系统审计（2026-07-13）

> 审计对象：`D:\张靖皋大桥\03_猫道动力分析\附件2-3全模态精确对齐_V2.0`  
> 审计边界：只读静态解析、现有 PREP7/POST1 输出复核、哈希与日志检查；**未执行 SOLVE**。  
> 明确非目标：不改材料倍率，不改索弹性模量，不加弹簧，不做频率拟合，不做参数灵敏度。  
> 当前总判定：**停止调参是正确决定。现模型至少存在三类先于参数的设置问题：BEAM188强弱轴方向写反、ASEC剪切刚度口径错误/未封板、CERIG与NLGEOM正式不兼容。全部历史动力结果已永久删除，现状没有动力基线。**

## 0. 先给结论

### 0.1 确认正确

1. **单位和全局坐标内部一致。** 当前主链统一采用 N-mm-tonne-s，X 顺桥、Y 横桥、Z 竖向；E=120000/206000 N/mm²、重力 `ACEL,0,0,9806`、密度约 `10^-8 tonne/mm³` 与坐标量级相容。APDL 主链未发现 `CSYS/LOCAL/ESYS/NROTAT` 改写，当前 D、CP、CERIG 均按全局笛卡尔自由度解释。
2. **11 个完整 V2 include 快照与当前源文件逐字节一致。** 九个基础 include 与 `第一阶模态验证_V1.0` 源一致，有限门架/横通道及空间质量两个 V2 include 也与各自生成源一致。
3. **LINK180 的关键设置正确。** 最终活动索单元为 TYPE 4/LINK180；`KEYOPT,4,2,1` 保持刚性截面面积，LINK 截面 30/32/33 均用 `SECCONTROL,,1` 明确设为 tension-only。73692 个 LINK180 单元均有一一对应的正初始应力，未发现缺号、重复或非正值。
4. **BEAM188 拓扑和定向节点几何没有退化，但这不等于截面轴设置正确。** TYPE 6 和 TYPE 70 均用 `KEYOPT(3)=3` 三次插值；TYPE 70 的 17679 个有限构件都有第三节点，且 K 向量与梁轴正交。TYPE 6 的 48620 个旧横梁使用方形 HREC 截面，对 y/z 互换不敏感；TYPE 70 中的非对称截面轴错误见下节，不能再列为“方向正确”。
5. **ROTY 数值稳定设置可追溯且没有滥加。** 旧横梁图共有 2860 个独立连通分量；`apply_modal_roty_stabilization_xlong.inp` 恰好给每个分量一个 `ROTY=0`，无分量漏锁、无重复锁、无锁到横梁外节点。
6. **显式拓扑闭合。** 完整 V2 为 109086 节点、172994 单元；活动单元为 LINK180 73692、BEAM188(TYPE 6) 48620、BEAM188(TYPE 70) 17679、MASS21 33003。静态解析未发现零长单元、重复无向端点对、缺失节点引用或完全未被任何单元引用的节点；把单元、CP、CERIG 作为运动学连接后，91407 个物理节点组成一个连通分量。
7. **显式约束不存在直接重复或同自由度冲突。** D 共 3968 条（UX 180、UY 464、UZ 464、ROTY 2860），CP 共 12 条，CERIG 共 5078 条；没有重复 D(node,dof)、没有 D 与 CP 同自由度交叠、没有节点在同一自由度进入多个 CP、没有重复 CERIG 主从对，也没有 CERIG 主节点反过来作为从节点形成显式环。
8. **质量命令链在内部闭合。** 最终活动质量由 LINK180 分布质量 3144.65552679 t 与 33003 个 TYPE 71/MASS21 平动质量 963.81138079 t 构成，总质量 4108.46690758 t。TYPE 71 使用 `KEYOPT(3)=2`（三维、无内禀转动惯量）且实常数按“质量”解释；材料 3/4 与 61～66 密度归零，避免梁自重与空间化质量重复。现有静力闭合文件给出的质量和竖向反力闭合到数值误差量级。
9. **初始状态—恒载—重力—摄动的顺序正确。** 初始应力在有限构件和质量前载入；权威恒载先清旧质量/清节点力并定义最终等效密度和 FZ，空间质量再删 FZ、重建 MASS21，最后施加重力。基态使用 `NLGEOM,ON`，随后按 `ANTYPE,...,PERTURB → PERTURB,MODAL → SOLVE,ELFORM → MODOPT/MXPAND → SOLVE` 的官方两阶段线性摄动顺序组织。
10. **拟采用的求解器与质量矩阵口径合理。** `LUMPM,OFF`对可一致化的结构单元采用一致质量矩阵；后续新作业拟采用Block Lanczos。实际求解设置必须由新作业manifest重新证明。

### 0.2 明确错误

1. **P0：5078 条 CERIG 与 `NLGEOM,ON` 是官方明确不支持的组合。** 这些命令展开为21096条线性约束方程；历史日志已原生警告“Constraint equations may not be valid for elements that undergo large deflections”。ANSYS 对 CERIG 的说明明确写明其基于小挠度理论、restricted to small-deflection analysis。完整 V2 又必须用 NLGEOM 处理索体系，因此不能以“静力收敛”替代运动学合法性。该项属于模型设置错误，不是频率问题。
2. **P0：BEAM188 方向节点与截面惯性轴的实现相反。** 生成器 `builder\build_finite_gate_passage_apdl.py:1188-1201` 写的是“让局部 y 轴接近全局 Z”，实际却把 K 节点放在投影后的全局 Z 方向。BEAM188 官方定义是 I-J-K 平面包含局部 x、z 轴，因此实际得到的是 **local z≈global Z**。首根门架下横梁 E2000001 可直接复核：I→J 沿 `-global Y`，I→K 沿 `+global Z`，故 local y=`+global X`。截面61却输入 `Iyy=9.8309E6`（弱轴）、`Izz=2.8165E7`（强轴），使竖向弯曲调用弱轴，实际惯量只有预期强轴的约 **34.9%**。明确影响 2698 根 gate_bottom 梁；2898 根 RHS50×30 梁也必须按真实50/30朝向复核。该错误足以让后续用 E 倍率“补刚度”的做法失去物理意义。
3. **P0：ASEC 剪切刚度采用默认 1.0，圆管口径不对。** 六类新梁的 `SECDATA` 只写 A/I/Iw/J，未显式给横向剪切刚度；原生日志显示 ASEC 的两个剪切修正均为 1.0。BEAM188 官方给出的薄壁空心圆管典型剪切修正为 0.5。材料63～65共 10521 根圆管梁因此至少缺少正确的剪切口径，当前横向剪切刚度可能约高估一倍。必须按截面类型显式定义并用单构件解析校核，不能靠全模频率倒推。
4. **P0：动力执行链尚未建立。** `prepare_and_run_v2.py`当前已硬禁用，底层仍使用共享目录和固定jobname；独立run_id、唯一jobname和逐文件SHA-256 manifest完成前不得恢复。

### 0.3 高风险待源证

1. **12 组 downpull CP。** 四个接口各用 UX/UY/UZ 三组 CP，把接口节点与 16 根底索节点作相同平动；每组 17 节点、横桥跨度 5340 mm。它可能是下拉索合力的必要等效分配，也可能抑制塔位截面畸变和扭转。无原设计接口详图前不得判为正确，也不得用材料参数补偿它。
2. **TYPE 70 的开口 H 门架下横梁未启用受限翘曲。** BEAM188 `KEYOPT(1)` 保持默认 0；截面 61 虽给出非零翘曲常数，当前不设 WARP 自由度。是否应考虑受限翘曲取决于端部连接和节点构造，现有模型内没有设计依据。
3. **BEAM188 的截面轴向伸长缩放未显式封板。** TYPE 6/70 未设置 `KEYOPT(2)`，NLGEOM 下使用默认的随轴向伸长缩放截面；若工程意图是经典刚性钢截面，应由设计口径确认后显式设定，而不是依赖默认值。
4. **MASS21 没有内禀转动惯量。** 33003 个质量点通过空间分布产生关于全局轴的“轨道”惯量，但单个门架部件、滚轮等绕自身质心的转动惯量未建模。对全桥低阶整体运动可能次要，但没有构件惯量源证就不能称为完整质量矩阵。
5. **42 组重合坐标节点。** 每组恰有两个节点，均出现在有限通道与基础索网接口，并通过共同 CERIG 主节点连接；当前没有形成零长或重复边。该做法在拓扑上自洽，但仍需接口详图证明“分节点+刚性约束”而不是应直接共节点。
6. **数值条件警告。** 历史静力日志报告单元矩阵最大/最小系数比超过 `1e8`；虽稀疏直接求解最小主元仍为正且基态收敛，但必须在修复 CERIG 后重新复核条件数/主元，不得把该警告视为纯性能问题。
7. **外部来源哈希仍不完整。** builder 输出自身有拓扑审计，但全部原始几何、FreeCAD 横通道文件、门架连接详图和实际命令行参数未形成一份完整输入哈希清单。
8. **全局 D 方向在大转动下的物理含义待确认。** 284 个“UY 已约束、UX 自由”的滑移支点采用固定全局方向；若支承局部轴随构形变化，现有全局 D 可能不等同于实际导向。需要支座局部轴/构造源证。
9. **阶段 B 工况4证据缺稳定归档。** 79,460 节点阶段 B 与 109,086 节点 V2 身份已在最新文字中区分，但阶段 B 三维自由 UY 工况4的稳定输入—日志—结果—哈希链仍未在正式工作区闭合。
10. **梁端释放、偏心与刚接口径未封板。** 完整装配没有`ENDRELEASE`、没有`SECOFFSET`；新梁端通过共享节点或CERIG按刚接处理。设计图中的销轴、螺栓、焊接和偏心构造尚未形成逐接口台账，不能把所有连接默认刚接当成已证实事实。
11. **扭转惯量上界脚本口径待修正。** `audit_mass_torsional_inertia_bounds.py`把外缘横坐标硬编码为24120 mm，但现有MASS21节点已到`|Y|=25140 mm`。应按实际质量分布重新形成惯量台账。
12. **目标报告与当前V2不是同一套已证明等价的建模口径。** 附件报告只明确说明索用LINK10、梁/门架用BEAM4、部分附属构件只作均布质量；当前V2采用LINK180、BEAM188、详细有限门架/横通道和空间化MASS21。缺少源输入时不得假设严格等价。

### 0.4 建议修复顺序（全部为非调参修复）

1. **P0 保持清零状态：**历史动力二进制、频率和振型均已永久删除；新作业建立前不得恢复或拼接旧结果。
2. **P0 纠正截面轴：**先修生成器对 BEAM188 K 节点的轴定义，或按实际 local z 重新排列 Iyy/Izz；对2698根H175和2898根RHS50×30生成逐构件方向台账，并用单梁解析结果证明强弱轴，而不是看全模频率。
3. **P0 纠正 ASEC 剪切：**按 H、RHS、圆管分别显式给剪切刚度/修正系数；对63～65圆管用0.5物理基准做单构件解析核验。该步骤是截面定义修复，不是参数标定。
4. **P0 修复运动学：**在不改变材料、截面和质量的前提下，把 CERIG 刚域替换为支持大转动的 surface-based MPC/接触 MPC，或用明确有限杆件实现；若坚持 CERIG，必须证明全过程小转角并把分析边界降为小挠度，但这通常与索体系 NLGEOM 需求冲突。
5. **P0 建立独立作业身份：**每个基线/诊断使用独立目录、独立 jobname、独立 input/out、独立 manifest；禁止任何诊断继续写 `attachment23_v2` 前缀。
6. **P0 生成真正零错误基线入口：**把当前 runner 的“准备输入”改为独立 staging 目录且真正只读预检；固定主输入版本、include 哈希、实际命令行、MAPDL 可执行文件哈希与进程数。此步骤只改执行链，不改物理参数。
7. **P1 封板元素设置：**根据连接详图裁定 BEAM188 `KEYOPT(1)` 翘曲、`KEYOPT(2)` 截面缩放、梁端释放与 `SECOFFSET`；记录截面局部轴规则和门架下横梁端部约束。
8. **P1 封板 downpull 接口：**依据原设计节点/刚臂/合力传递图，书面确认 12 组 CP，或改为可随大转动更新的有限连接；非共点CP会产生官方文档所述人工力矩，不能先用频率试算倒推其合理性。
9. **P1 完成质量源证：**给所有集中质量分项补质量、质心、内禀转动惯量及“已由分布密度/已由 MASS21”互斥标志；保持 4108.46690758 t 闭合，但不以闭合代替分布正确性，并修正25140 mm惯量边界。
10. **P1 修复机器审计：**新增同jobname多输入检测、主输入/二进制/重启动文件哈希、后处理唯一job绑定及阶段B/V2身份字段。
11. **P1 建立目标模型差异表：**逐项对照附件报告的LINK10/BEAM4/均布质量口径与当前LINK180/BEAM188/MASS21口径；没有源输入的项目明确标记“待源证”，不得用参数拟合替代等价证明。
12. **P2 仅在上述完成且另行授权后重放：**必须从头重算修复后的 NLGEOM 静力基态，再以相同 MAPDL 版本与 4 进程生成独立、0-error、带 manifest 的基准摄动。当前任务未执行该步骤。

## 1. 审计身份与证据边界

### 1.1 完整 V2

- 新作业入口：尚未建立；`prepare_and_run_v2.py`当前硬禁用
- 节点/单元：109086 / 172994
- 有限门架/横通道：TYPE 70/BEAM188，17679 单元
- 完整装配 CP：12 组，均来自 `apply_mct_downpull_equivalent_xlong.inp`
- 当前动力频率、振型和二进制结果：0项，历史文件已永久删除。

### 1.2 阶段 B

- 身份：79,460 节点旧 CP 模型；门架采用三向 CP，21 品横通道另有 `CP,UY`。
- 允许用途：支持“全局 UY 应释放”的边界方向判断及阶段 B 自身的横移/索力回归。
- 禁止用途：证明 V2 有限门架/横通道拓扑、V2 扭转刚度、V2 工况4或最终模态。

## 2. 单位与坐标

| 项目 | 当前设置 | 审计结果 |
|---|---|---|
| 力 | N | 与 E、重力、节点力和反力输出一致 |
| 长度 | mm | 全桥 X 约 4.2×10⁶ mm，门架/通道构件尺寸为 mm |
| 质量 | tonne | `tonne·mm/s²=N`，与 ACEL=9806 相容 |
| 时间 | s | 模态频率输出单位 Hz |
| APDL 轴 | X 顺桥、Y 横桥、Z 竖向 | 主链无局部坐标改写 |
| CAD→APDL | `Xnew=Yold, Ynew=-Xold, Znew=Zold` | 生成器与节点抽查相符；仍需把原始 CAD 输入纳入完整哈希清单 |

源证：`builder\generated\build_audit.json`、`mass21_spatialization_audit_v2.json`及活动装配include。

## 3. ET、KEYOPT、截面轴与翘曲

| 最终活动类型 | 单元 | 数量 | 关键设置 | 判定 |
|---|---:|---:|---|---|
| TYPE 4 | LINK180 | 73692 | `KEYOPT(2)=1`；LINK 30/32/33；`SECCONTROL,,1` | tension-only、刚性面积正确 |
| TYPE 6 | BEAM188 | 48620 | HREC 40/41；`KEYOPT(3)=3` | 三次插值；全部沿全局 Y，方形截面对默认轴不敏感 |
| TYPE 70 | BEAM188 | 17679 | ASEC 61～66；`KEYOPT(3)=3`；第三定向节点 | **方向节点不退化，但K节点轴解释错误；H175强弱轴已对调，RHS50×30待逐件复核；ASEC剪切未封板** |
| TYPE 71 | MASS21 | 33003 | `KEYOPT(3)=2` | 三维平动质量，无内禀转动惯量 |

非活动但仍定义的 TYPE 1/LINK10、TYPE 2/BEAM4 没有最终活动单元；它们的保留本身不造成刚度或质量贡献，但正式审计应区分“已定义”和“有活动单元”。

关键源证：

- `run\convert_crossbeams_beam4_to_beam188.inp:7-25`
- `run\apply_mct_authoritative_initial_state_link180.inp:7-40`
- `run\apply_finite_gates_and_passages_v2.inp:8-51`
- [ANSYS LINK180](https://ansyshelp.ansys.com/public/Views/Secured/corp/v242/en/ans_elem/Hlp_E_LINK180.html)
- [ANSYS SECCONTROL](https://ansyshelp.ansys.com/public/Views/Secured/corp/v242/en/ans_cmd/Hlp_C_SECCONTROL.html)
- [ANSYS BEAM188](https://ansyshelp.ansys.com/public/Views/Secured/corp/v242/en/ans_elem/Hlp_E_BEAM188.html)

### 3.1 截面方向的直接反证

BEAM188 的 K 节点定义 I-J-K 平面包含局部 x、z 轴。生成器却把“希望成为 local y 的参考向量”直接拿来放置 K 节点，因此把参考方向赋给了 local z。

代表单元 E2000001：

- I=2000001：`(89913.65,24525.00,72640.661)`；
- J=2000002：`(89913.65,24360.00,72640.661)`，局部 x 沿 `-global Y`；
- K=2000021：`(89913.65,24525.00,72740.661)`，I→K 沿 `+global Z`；
- 因此 local z=`+global Z`、local y=`+global X`，并非生成器注释所称 local y≈global Z。

截面61的 `Iyy=9.8308997E6`、`Izz=2.8164706E7 mm^4` 因而使门架下横梁的竖向弯曲落在弱轴。该结论由官方轴定义与节点坐标直接推出，不依赖模态频率、参数拟合或主观判断。

### 3.2 ASEC 剪切与翘曲

- 六个 ASEC 未显式输入横向剪切刚度，日志回显剪切修正均为1.0；
- BEAM188 官方给出的薄壁空心圆管典型剪切修正为0.5，材料63～65共10521根；
- TYPE70仍为 `KEYOPT(1)=0`，截面61虽输入非零 Iw，也未激活 WARP 自由度；
- 当前没有 `ENDRELEASE` 和 `SECOFFSET`，所有端接/偏心均按共享节点或刚域处理。

所以“ASEC保留A/I/J/Iw”只能表示数值被写入，不能表示剪切、翘曲、轴向和连接行为已经正确生效。

## 4. 材料、截面、实常数与密度

### 4.1 最终材料口径

- 材料 1/2：索材 E=120000 N/mm²，最终密度分别为 `1.264848052212931E-08`、`8.598817050785234E-09 tonne/mm³`。
- 材料 5：下拉索等效 E=120000 N/mm²，密度与材料 1 相同。
- 材料 3/4：旧大/小横梁 E=206000 N/mm²，最终密度清零；质量改由 TYPE 71 表达。
- 材料 61～66：有限门架/横通道 E=206000 N/mm²、PRXY=0.31、密度清零；质量改由 TYPE 71 表达。

本次没有改动任何 E 或密度。材料 1/2/5 的“等效密度”是质量映射参数，不应被表述为钢材真实密度。

### 4.2 截面与实常数

- LINK 截面 30/32/33 面积均大于零，分别分配给底承重索、门架承重索和下拉索。
- HREC 40/41 替代 BEAM4 的 R3/R4 截面刚度；BEAM188 忽略旧梁实常数，R3/R4 留存不产生重复截面贡献。
- 旧 LINK10 的 R1/R2、R1000～2053、R50000～50001 在 LINK180 转换后删除。
- MASS21 使用连续 R1000～R34002，共 33003 组，和 33003 个质量单元一一对应。

## 5. 节点、单元拓扑与组件

| 检查项 | 结果 |
|---|---:|
| 节点 | 109086 |
| 单元 | 172994 |
| 两节点结构单元 | 122312 |
| 三节点定向 BEAM188 | 17679 |
| 单节点 MASS21 | 33003 |
| 零长结构单元 | 0 |
| 重复无向端点对 | 0 |
| 缺失节点引用 | 0 |
| 未被任何单元引用节点 | 0 |
| 重合坐标组 | 42 组/84 节点 |
| 单元+CP+CERIG 后物理连通分量 | 1 |
| 组件 CM | 348 个，名称唯一且长度≤15 |

42 组重合坐标节点均应保留为“接口待源证”，不能简单 `NUMMRG`；在缺少连接详图前合并节点可能改变转动和刚性区运动学。

## 6. D、CP、CERIG、ROTY 与大变形

### 6.1 计数与直接冲突

| 类型 | 数量 | 细分 | 直接冲突 |
|---|---:|---|---|
| D | 3968 | UX 180、UY 464、UZ 464、ROTY 2860 | 0 |
| CP | 12 | UX/UY/UZ 各4组；每组17节点 | 0 个成员重复；0 个 D-CP 同自由度交叠 |
| CERIG | 5078 | ALL 1954、UXYZ 3124 | 0 个重复主从对；0 个主从显式环 |

### 6.2 大变形合法性

CERIG是本次最严重的设置问题。官方文档说明其生成的线性约束方程基于小挠度理论，且不支持large-deflection。当前NLGEOM不能为了保留CERIG而关闭，因为LINK180索体系、tension-only与应力刚化均依赖非线性基态。

建议的非调参修复是更换连接运动学，不是修改刚度：优先采用可随当前构形更新的 surface-based MPC；若用有限杆/梁连接，应按真实刚臂和释放构造建模。

官方依据：[ANSYS CERIG](https://ansyshelp.ansys.com/public/Views/Secured/corp/v251/en/ans_cmd/Hlp_C_CERIG.html)、[Constraint equations and large rotation](https://ansyshelp.ansys.com/public/views/secured/corp/v251/en/ans_mod/Hlp_G_MOD12_7.html)。

### 6.3 downpull CP 的人工力矩风险

四个塔位接口各把一个参考节点与16根底索节点在 UX/UY/UZ 上分别同位移耦合。单组16根底索横桥跨度5340 mm，并非共点。ANSYS 官方 CP 文档明确说明：非共点、且不沿耦合位移方向排列的节点会形成额外的 artificial moment constraint。当前12组CP因此不是普通“合力分配”，而是会钳制塔位截面转动/畸变的运动学假设，必须由真实鞍座/下拉索构造证明。

官方依据：[ANSYS CP](https://ansyshelp.ansys.com/public/Views/Secured/corp/v251/en/ans_cmd/Hlp_C_CP.html)。

## 7. 质量来源与 MASS21

### 7.1 当前质量分解

| 来源 | 质量/t | 说明 |
|---|---:|---|
| LINK180 分布质量 | 3144.655526792727 | 材料 1/2/5 等效密度 |
| TYPE 71/MASS21 | 963.811380787273 | 33003 个空间化平动质量 |
| 总质量 | 4108.466907580000 | 与现有精确质量摘要一致 |

### 7.2 重复与遗漏判断

- 旧 TYPE 3 质量在权威恒载 include 中删除；当前完整基础网格实际无活动 TYPE 3。
- 材料 3/4、61～66 密度归零，避免梁自重与 MASS21 双计。
- 权威 FZ 先用于生成质量分配，空间质量 include 最后 `FDELE,ALL,FZ`，静力中依靠 MASS21×ACEL 还原其重量，避免 FZ 与惯性重力双计。
- 总质量闭合只能证明“数量不重不漏”，不能证明每个分项的空间位置和内禀转动惯量正确。

TYPE 71 的 `KEYOPT(3)=2` 意味着只含 UX/UY/UZ 质量。官方依据：[ANSYS MASS21](https://ansyshelp.ansys.com/public/Views/Secured/corp/v251/en/ans_elem/Hlp_E_MASS21.html)。

### 7.3 惯量边界纠错

旧脚本把质量外缘固定写成 `|Y|=24120 mm`，但当前空间化质量表已有 `|Y|=25140 mm` 的节点。因此旧报告中的1.089292不是严格几何上界；按实际最大横坐标复算约为1.115499。该纠错不授权质量外移，也不改变“先修设置、禁止调参”的顺序。

## 8. INISTATE、重力、荷载与摄动命令链

### 8.1 INISTATE

- 定义数：73692；唯一单元号：73692。
- 覆盖：1～73688 及 400000～400003，恰好覆盖全部最终 LINK180。
- 应力范围：66.494484～705.166933 N/mm²，全部为正拉应力。
- `INISTATE,SET,DTYP,STRE` 与后续逐单元 DEFINE 顺序正确。

### 8.2 基态荷载

- `ACEL,0,0,9806` 使结构惯性力沿 -Z，符合 Z 向上约定。
- `AUTH_TOT2` 反力目标与总质量×9.806 的单位换算一致。
- `NLGEOM,ON` 自动包含应力刚化；`PSTRES,ON` 在基态前设置，未发现命令顺序错误。

### 8.3 线性摄动

当前顺序符合官方两阶段过程：

```text
ANTYPE,STATIC,RESTART,,,PERTURB
PERTURB,MODAL,AUTO,CURRENT,PARKEEP
SOLVE,ELFORM
LUMPM,OFF
MODOPT,LANB,...
MXPAND,...
SOLVE
```

`PARKEEP` 保留位移约束/CP/CE，删除外部机械荷载；基态几何与应力通过 restart snapshot 进入切线刚度。官方依据：[ANSYS PERTURB](https://ansyshelp.ansys.com/public/Views/Secured/corp/v242/en/ans_cmd/Hlp_C_PERTURB.html)。

## 9. 求解器、质量矩阵、输出、restart、作业名与 include

### 9.1 include 顺序

1. 基础索网与旧横梁；
2. BEAM4→BEAM188；
3. downpull；
4. MCT D；
5. LINK180 + INISTATE；
6. V2 有限门架/横通道；
7. ROTY；
8. 代表索组件；
9. 权威恒载；
10. V2 空间 MASS21；
11. 重力。

该顺序目前正确，且没有混入阶段 B 的门架/横通道 CP include。

### 9.2 求解器和质量矩阵

- 静力：保留的活动静力文件来自现有V2静力基态；新作业须在manifest中显式冻结方程求解器。
- 模态：后续拟采用Block Lanczos和质量归一化，尚无新结果。
- `LUMPM,OFF`：一致质量矩阵；MASS21 自身按其定义形成集中质量。
- 历史日志系数比超过 `1e8`，应在运动学修复后复核。

### 9.3 输出和 restart

- 静力只保留 LAST，restart 也定义为 LAST，足以支撑最后平衡点摄动。
- 新模态作业应输出完整NSOL，并按需要决定是否在`MXPAND`中计算单元结果；不得由节点振型推称已有模态单元应力/能量。
- 当前runner已硬禁用；独立run_id、唯一jobname和完整manifest完成前不得生成新输入或求解。
- 后处理已改为要求显式频率源、job manifest和逐阶向量哈希，仍需由首个新作业验证。

### 9.4 作业名和身份

正式重放前必须执行“新目录+新jobname+新manifest”；不能依赖`/EXIT,NOSAVE`防止结果文件覆盖。

## 10. 当前保留的静力证据

| 证据 | 当前身份 | 可用性 |
|---|---|---|
| `attachment23_v2.rdb/ldhi/rst/四个r001` | 完整 V2 静力基线 | 哈希与诊断归档一致，可冻结；本次未重启动 |
| `attachment23_v2_equilibrium.db` | 完整 V2 平衡数据库 | 可冻结；不是当前变体实际显式读取的唯一入口 |
| 阶段 B 约22 m工况4 | 79,460节点旧CP模型，稳定归档未闭合 | 不得外推完整 V2 |

历史动力日志、频率、振型、RSTP/MODE/FULL/EMAT/ESAV及派生结果已经永久删除，不在本表保留。

## 11. 验证结果与验收状态

### 11.1 已通过的静态审计

- 单位/坐标内部一致；
- include 当前哈希一致；
- LINK180 tension-only/面积/INISTATE 覆盖正确；
- BEAM188 定向节点几何无退化，但截面轴方向反证未通过；
- 节点/单元数量闭合；
- 零长、重复边、缺节点、孤立物理装配体均为0；
- D/CP/CERIG 没有直接重复或同自由度交叠；
- 质量总量闭合；
- 静力—摄动命令顺序正确。

### 11.2 未通过/阻断

- CERIG 与 NLGEOM 的形式合法性：**未通过**；
- 门架下横梁 H175 强弱轴方向：**未通过**；
- ASEC 圆管剪切修正：**未通过**；
- 正式0-error基线：**不存在**；
- 当前通用二进制基线身份：**失效**；
- runner 与 run 主输入同代性：**未通过**；
- downpull CP 物理源证：**缺失**；
- BEAM188 翘曲/截面缩放源证：**缺失**；
- 梁端释放/偏心与RHS50×30实际朝向源证：**缺失**；
- 集中质量内禀转动惯量源证：**缺失**；
- 阶段 B 工况4稳定归档：**缺失**。

## 12. 审计复现说明

本报告对应的保留机器台账为`模型设置系统审计_证据台账_20260713.csv`。含历史动力字段的旧JSON及旧产物哈希表已随历史结果清理删除。

后续复核必须保持：

1. 不运行 `prepare_and_run_v2.py`、builder、质量生成器或 post 生成器作为“只读检查”；
2. 不运行任何含 SOLVE 的 APDL；
3. 不恢复或重建已删除的历史动力结果，阶段B静力证据保持独立；
4. 新审计只追加新文件/新历程段，并以 SHA-256 绑定输入身份。

## 13. 未决分歧与建议

### 未决分歧

1. downpull 的 12 组 CP 是必要合力接口还是过度钳制；
2. 门架下横梁是否需要受限翘曲；
3. BEAM188 是否应显式采用刚性截面缩放；
4. 42 组重合接口应保持分节点刚域还是直接共节点；
5. 集中构件内禀转动惯量是否可忽略；
6. 284 个滑移支点应采用固定全局方向还是局部随动方向。

### 建议

应先裁定并授权运动学/模型设置修复，不授权任何无来源参数修改。完成修复后，再以独立作业名生成0-error静力基线；频率比较继续保持在本模型设置审计之外。
