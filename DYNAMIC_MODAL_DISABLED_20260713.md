# 当前动力模态链已禁用

全部历史动力模态作业及派生结果已于2026-07-13按用户指令永久删除，不保留退休目录、旧结果清单或可恢复归档。活动目录仅保留模型输入、静力证据、方法文件和未来单作业后处理代码。

禁用原因：

1. 多个作业复用同一`/FILNAME,attachment23_v2`并覆盖通用二进制；
2. 旧后处理会自动搜索并逐阶汇总同目录中的多份频率来源；
3. 频率、向量、二进制和输入之间缺少唯一manifest及逐文件哈希绑定；
4. 当前没有可接受的动力模态单作业结果。

`prepare_and_run_v2.py`已设置为硬失败。除非先建立独立run_id目录、唯一jobname和完整manifest，并获得明确授权，否则不得恢复。
