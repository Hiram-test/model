# 后续 Pro 模型执行入口与优先级

## 使用目的

本文件是后续 Pro 模型或新执行线程的第一入口。它用于快速确定当前真实状态、下一步工作顺序、验收口径和禁止事项，避免重新经历“二维 Pilot 被误认为完整三维模型”“schema 通过被误认为工程通过”等问题。

详细历史、失败记录和数值依据见：[完整执行日志](docs/zhangjinggao-catwalk-automation-execution-log-2026-07-22.md)。

## 当前真实状态

| 项目 | 当前状态 |
|---|---|
| 施工图整册提取与缺页检测 | 已执行；MD1-06 实体缺页 |
| 旧十九节点统一运行器 | 19/19 主工件执行并通过 schema，但计算内核是二维等效索束 |
| 独立完整三维模型 | 已建立并真实调用 ANSYS MAPDL 求解 |
| 三维模型规模 | 81,312 节点、128,522 单元 |
| MAPDL 求解 | 7/7 子步收敛，主求解 0 error |
| 三维结果与已有资料比较 | 已执行，但未通过工程一致性验收 |
| 主要几何差异 | 主跨跨中高程低 25.56 m |
| 十九节点三维职责状态 | 4 PASS、9 PASS_WITH_BOUNDS、6 BLOCKED |
| 完整三维一键十九节点运行目录 | 0 个；尚未正式接线 |
| 工程发布 | BLOCKED |

## 模型正确性判定

后续 Pro 模型必须沿用以下四层判断，不得用单一“对/不对”覆盖：

1. **三维拓扑与数量基本正确**：两幅猫道、底索、门架索、扶手索、横梁、142 榀门架和 12 道横通道已经真实入模。
2. **有限元编译和求解执行正确**：LINK180、BEAM188、MASS21 已编译，ANSYS 真实运行并收敛，结果可以提取。
3. **设计线形和初始状态尚不正确**：主跨跨中高程、门架索独立线形和初始索力没有与参考资料闭合。
4. **正式工程模型尚未完成**：缺少或未确认 MD1-06、正式钢丝绳参数、端部转索与下拉、鞍座摩擦、精确截面、施工阶段、温度和风工况。

当前模型应称为“完整三维自动化执行验证模型”，不得称为“已通过复核的生产模型”。

## 后续工作必须分成两条路线

### 路线 A：参考校准与实现复现

目的：验证三维 FEM 实现能否复现现有复核报告和 MCT。

允许使用：

- 复核报告表 1-5 的猫道底索控制点；
- 复核报告表 1-6 的门架索控制点；
- MCT 初始力、无应力长度和已有荷载口径；
- 既有 ANSYS 的同类结果。

必须明确标记：

- `REFERENCE_CALIBRATED`；
- 该路线不是独立找形；
- 目标是复现 FEM 实现，不是独立验证设计线形。

### 路线 B：正式资料独立验证

目的：在不读取参考结果的前提下，由正式图纸和批准参数重新找形并求解。

必须先取得：

- MD1-06 或等效正式控制点确认单；
- 底索与门架索的正式端点、转索点、下拉点和下料信息；
- 正式钢丝绳弹性模量、金属面积、单位质量和破断力；
- 批准的荷载、组合、温度、风和施工阶段定义。

独立运行必须先封存，再读取路线 A 或既有结果比较。不得用参考索力、无应力长度或完整成型坐标反向调参。

## 优先级规划

### P0：把完整三维适配器接入十九节点统一运行器

目标：一条命令完成图纸证据、三维模型、ANSYS 求解、验证、比较和 N00–N18 工件输出。

具体任务：

1. 为完整三维运行定义稳定输入清单和运行目录契约。
2. 将三维节点、曲线、构件族和横向连接映射到 N07 `fem_geometry_ir`。
3. 将索、梁和质量属性映射到 N08 `property_bundle`。
4. 将 280 个支承节点、构件连接和简化假定映射到 N09。
5. 将底索、门架索和扶手索的初始状态分别映射到 N10。
6. 将恒载和后续工况映射到 N11，不得只登记工况名称而不生成实际荷载。
7. 将 128,522 单元、单元族和数值控制映射到 N12。
8. 将数量闭合、单位检查、初始力数量级和边界检查映射到 N13。
9. 将实际 MAPDL 命令、哈希、退出码、日志和 RST 映射到 N14。
10. 将位移、分索族轴力、反力、质量闭合和警告处置映射到 N15。
11. N16–N18 必须根据真实缺口阻断发布，不得因 schema 通过自动放行。

完成标准：

- 产生一个新的完整三维十九节点运行目录；
- 19 个主工件全部通过 schema；
- N07–N15 的数量和结果与独立三维运行完全一致；
- N18 在资料未补齐前仍为 `BLOCKED`；
- 不再引用旧二维 Pilot 的 161 节点、160 单元作为三维证据。

### P1：建立路线 A 的 V3 参考校准模型

目标：先判断 25.56 m 线形差异、初始索力和边界路径分别贡献多少。

具体任务：

1. 底索和门架索使用各自独立的控制点，不再共享一条线形加固定竖向偏移。
2. 展开锚固点、两级转索点、塔顶分跨点、主跨下拉点和南辅塔转索点。
3. 采用与 MCT/复核报告一致的坐标原点、构件族和荷载口径。
4. 分别复现底索、门架索的初始力和无应力长度。
5. 按同一构件族、同一位置、同一工况比较位移和轴力。

完成标准：

- 控制点逐点对齐并登记来源；
- 底索和门架索无应力长度使用同一端点口径比较；
- 反力和外荷载闭合；
- 全部 LINK180 保持受拉；
- 与参考结果的差异必须解释到几何、初始状态、质量、边界或荷载，不得笼统称为软件误差；
- 未经项目批准，不预设任意百分比作为工程验收阈值。

### P2：补齐正式资料并执行路线 B

目标：获得真正独立、可用于工程复核的线形和初始状态。

具体任务：

1. 取得 MD1-06 或正式等效资料。
2. 解决施工图与复核报告中的 φ50/φ54、单位质量和面积冲突。
3. 取得正式钢丝绳弹性模量及产品参数。
4. 用新资料创建全新 run，不覆盖现有独立封存结果。
5. 在封存前隔离 MCT、既有 ANSYS 和报告结果。

完成标准：

- G1–G3 的关键缺页或冲突关闭；
- 找形控制点、曲线长度、无应力长度和初始索力全部可追溯；
- 封存清单包含输入、脚本和结果哈希；
- 封存后才执行参考比较。

### P3：增加正式结构和荷载复杂度

按以下顺序逐层增加，避免一次加入全部因素后无法定位差异：

1. 精确梁截面面积与惯性矩；
2. 端部转索、下拉与变位构件；
3. 鞍座接触、滑移或摩擦；
4. 集中构件和附属质量的精确位置；
5. 施工荷载；
6. 温度工况；
7. 风荷载；
8. 完整施工阶段和组合。

每增加一层，必须创建新运行并与上一层比较质量、反力、位移和分索族轴力变化。

### P4：规范验算和发布控制

目标：只有在模型和工况完整后才进行正式规范结论。

要求：

- N16 使用批准的规范、材料设计值和荷载组合；
- N17 具有独立复核人或独立算法签认；
- N18 检查来源关闭、模型版本、结果哈希、审批记录和适用范围；
- 任何关键来源、临时参数或模型降阶未关闭时，保持 `BLOCKED`。

## 下一位 Pro 模型的首个执行任务

如果完整三维代码和本地运行目录可访问，首个任务不是继续调线形，而是：

> 建立完整三维运行到 N07–N15 的适配映射，生成新的十九节点运行目录，并证明每个三维数量和 MAPDL 结果来自 `RUN-ZJG-FULL3D-STATIC-002` 或其不可覆盖的新后继运行。

原因：当前最大的自动化缺口是“真实三维内核”和“十九节点统一运行器”尚未正式接线。先完成接线，后续每次替换资料才能自动形成可审计 Gate 台账。

如果只能访问 GitHub 仓库、不能访问本地完整三维代码和证据，则首个任务应改为：

> 仅制定适配器接口和所需文件清单，不得伪造 N07–N15 工件或声称已经复现模型。

## 必查数字

后续运行开始前，应先核对下列基线数字是否与日志一致：

- 图纸物理页：124；
- 缺失关键图：MD1-06；
- 三维节点：81,312；
- 三维总单元：128,522；
- LINK180：81,088；
- BEAM188：44,574；
- MASS21：2,860；
- 门架：142；
- 横向通道：12；
- 求解收敛子步：7/7；
- 独立主跨跨中：87.740 m；
- 参考主跨跨中：113.300 m；
- 当前跨中差：-25.560 m；
- 独立最大总位移：913.488 mm；
- 参考最大总位移：44.509 mm。

数字不一致时，必须先说明运行版本、输入或模型范围变化，不得直接覆盖基线。

## 禁止事项

- 禁止把 `19/19 schema PASS` 写成“完整三维工程模型通过”。
- 禁止把二维 Pilot 的节点和单元数作为三维模型证据。
- 禁止把主缆控制点直接当作猫道底索控制点。
- 禁止把底索线形整体上移固定高度当作正式门架索线形。
- 禁止在封存前读取参考索力、无应力长度或完整成型坐标并反向调参。
- 禁止比较不同构件族、不同物理端点或不同荷载工况后给出“误差百分比”。
- 禁止删除失败运行、无效后处理或纠正通知。
- 禁止把 MAPDL 收敛等同于物理输入正确或工程发布通过。

## 预期交付物

下一阶段至少应交付：

1. 完整三维适配器代码及中文逐行注释；
2. 三维运行到 N07–N15 的字段映射说明；
3. 新的不可覆盖运行目录；
4. 十九份主工件及 schema 校验报告；
5. Gate 台账、问题台账和发布清单；
6. APDL deck、MAPDL 日志、RST 哈希和结构化结果；
7. 分索族、同端点、同工况的参考对比；
8. 明确的 `PASS`、`PASS_WITH_BOUNDS` 或 `BLOCKED` 结论。
