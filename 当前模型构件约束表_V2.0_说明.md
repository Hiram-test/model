# 当前模型构件约束表 V2.0 字段说明

- `模型版本`：标识本表对应的 ANSYS 动力模型版本，防止与旧 CP 约束模型混用。
- `坐标系`：说明 APDL 三个平动自由度在桥梁全局坐标中的物理方向。
- `构件间约束`：逐项记录四类构件之间是否直接连接、所用命令、关联自由度和传力路径。
- `连接双方`：列出当前连接或待判定关系涉及的两个构件。
- `ANSYS命令`：记录当前模型使用的核心 APDL 命令；`null` 表示不存在直接约束命令。
- `约束自由度`：列出被刚性关联或固定的自由度；空数组表示双方无直接自由度约束。
- `连接说明`：说明主从节点关系、间接连接路径或有限刚度构件作用。
- `MPC184 rigid beam direct elimination + coincident general joint PNLT=-5.0E10 N/mm`：先用六自由度直接消元刚臂把门架轴线运动传到索节点位置，再由共点三平移 general joint 以 5.0×10^10 N/mm 位移绝对罚因子高精度耦合索节点。
- `BEAM188 + MPC184 rigid beam; direct elimination`：有限刚度 BEAM188 杆系承担构件变形，短几何偏置由六自由度直接消元刚臂传递。
- 共点 general joint 的 `KEYOPT(1)=16` 允许用 `SECJOINT,RDOF,1,2,3` 指定三项相对平移，`KEYOPT(4)=1` 只激活 UX、UY、UZ，`KEYOPT(2)=1` 选择 penalty 公式；因此不向 LINK180 索节点引入转角自由度，也不会形成两层 direct-elimination 串联依赖。
- `连接参数.UXYZ位移绝对罚因子_N_per_mm=50000000000.0` 是生成器内部保存的正幅值，单位 N/mm；`SECJOINT_PNLT_APDL值_N_per_mm=-50000000000.0` 中的负号是 MAPDL 指定绝对罚因子的语法约定，不表示负刚度。
- `连接参数.共点相对滑移验收上限_mm=0.00001` 是当前独立算例门槛；`独立小算例最大相对滑移_mm=0.0000091944` 是同时包含静力与线性摄动步骤的实测值。该连接是经量化验收的有限罚刚度近似，不宣称数学上的零滑移。
- `索端边界约束`：记录两类索在锚碇端及主塔索鞍处的位移边界。
- `适用构件`：列出该边界条件覆盖的索类别。
- `位置`：说明边界条件所在的工程部位。
- `释放自由度`：列出未被固定、允许运动的自由度。
- `边界说明`：用工程语言解释对应的固定或滑移含义。
- `已取消的旧约束`：保留旧模型约束及其当前替代方式，防止新旧 include 同时使用。

JSON 使用标准语法，未加入注释；所有说明均通过字段值和本 Markdown 文件提供。
