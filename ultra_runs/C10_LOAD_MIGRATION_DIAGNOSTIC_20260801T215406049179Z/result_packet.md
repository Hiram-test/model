# C10 恒总荷载位置迁移 0.5% 步长终止结果

状态：`ABORTED_BY_CONTROLLER_AFTER_LS2_FIRST_0_5_PERCENT_MIGRATION_DIVERGED`。本运行没有得到完整静力结果，也没有启动模态。

- LS1 的 beta=1 旧荷载位置基态在第 4 次平衡迭代正式收敛。
- LS1 最终力残差为 `0.7009 N`，力矩残差为 `10.78 N·mm`；方程数恒为 `1,234,834`。
- 最小主元为正值 `25.3126539`，没有 small、zero 或 negative pivot；旧双层约束病态没有复发。
- LS2 使用 `200/200/200` 固定子步，首步仅从 beta=1 移到 beta=0.995，即迁移 0.5% 的二期重力位置。
- LS2 初始力残差为 `522.4 N`，第一次位移修正为 `-0.8899 mm`；第 2 次迭代随后跃升到力残差 `85.003 MN`、力矩残差 `1.5153 GN·mm`，下一位移修正扩大到 `-12.17 mm`。
- 第 3 次迭代开始时位移范数已增至 `17.09 mm`、转角范数增至 `0.014948 rad`，呈明确恶化趋势；继续迭代没有工程合理性。
- 已核对包装进程 PID 47584 和实际求解器 PID 30956 的完整命令行仅指向本运行与本作业，停止后两者及其控制台进程均已确认不存在。
- 结论：失败不再能归因于 5% 步长过粗；0.5% 路径也被证伪。下一条单差异验证按 MPC184 官方建议，仅在 LS2 迁移静力阶段设置 TYPE72 `KEYOPT(5)=1`，其余荷载、拓扑和收敛门不变。
