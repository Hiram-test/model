# A10 80 阶六组件 SENE 后处理 QA 报告

## 结论

状态为 **PASSED**。正式 MAPDL 作业仅以 `/POST1` 读取既有模态 DB 与合并 RSTP，输入中 `SOLVE` 命令数为 0、`/SOLU` 命令数为 0，最终以 `/EXIT,NOSAVE` 退出。正式 OUT 报告 0 条警告、0 条错误、1 个成功标记、0 个实际执行的失败标记。

源 DB 与 RSTP 的运行前后字节数、修改时间和 SHA-256 完全一致；新目录顶层未生成任何 DB、RST、RSTP 或分片结果文件。因此本次操作属于严格只读后处理，没有触发新求解，也没有改写旧文件。

## 组件集合门禁

| 组件 | 元素数 | 结果 |
|---|---:|---|
| `GATE_BOTTOM_E` | 2698 | 通过 |
| `GATE_TOPPOST_E` | 1562 | 通过 |
| `PASS_CHORD152_E` | 4011 | 通过 |
| `PASS_FRAME102_E` | 1890 | 通过 |
| `PASS_BRACE51_E` | 4620 | 通过 |
| `PASS_RHS5030_E` | 2898 | 通过 |

六组件计数和 = 17679，并集数 = 17679，TYPE70 总数 = 17679，并集与 TYPE70 的交集数 = 17679。计数和等于并集证明六组件互不重叠；并集、TYPE70 和交集三者相等证明六组件完整且恰好覆盖 TYPE70 集合。

## 80 阶数值门禁

- 总能量 CSV 为 80 行，模态阶次严格连续为 1–80；80 个总 SENE 全部为有限正数，范围为 `5.3254223066725621E-07` 至 `1.3029838624656029` N·mm。
- 六组件长表为 480 行，结构严格为 80 阶 × 6 组件；480 个组件 SENE 全部为有限非负数，范围为 `6.3632799305350937E-10` 至 `1.1197792589819811` N·mm。
- 480 个组件比例全部位于闭区间 0–1，实际范围为 `2.7891646385266810E-06` 至 `0.87758188483521538`。
- 由 CSV 重新计算 `组件 SENE / 总 SENE` 后，与输出比例的最大绝对误差为 `5.5511151231257827E-16`；长表总能量与 80 行总能量 CSV 的最大绝对差为 0。
- 每阶六组件比例和范围为 `0.000340602136629903` 至 `0.99623091653150719`，未超过全模型总能量。
- 与既有 `../solver/a10_gate_bottom_modal_sene.csv` 做 80 阶交叉验证：总能量、`GATE_BOTTOM_E` 能量及比例的最大绝对差均为 0，逐值完全一致。

## 源文件不变证据

- `cw_A10_0715t172214_24_modal.db`：272629760 字节；修改时间前后均为 `2026-07-16T03:04:21.7429400+08:00`；SHA-256 前后均为 `69EB6F94E69C8EF445826E2E3B0220E48BCC12D86793484D0FA486B243030B7B`。
- `cw_A10_0715t172214_24.rstp`：3444375552 字节；修改时间前后均为 `2026-07-16T02:59:35.9410236+08:00`；SHA-256 前后均为 `15A14E680929679D34840E3FB057C5D66C9503E1C3A766A9FA27379273712D78`。

## 输出说明

- `a10_sene6_mode_totals_numeric.csv`：80 行、2 列纯数值总能量表。
- `a10_sene6_modal_numeric.csv`：480 行、5 列纯数值六组件长表。
- `a10_sene6_counts_numeric.csv`：1 行、11 列纯数值组件与集合计数证据。
- `a10_sene6_post.out`：正式 MAPDL 主输出，含逐阶 `SET`/`SSUM` 证据、资源统计和成功标记。
- `qa_summary.json`：机器可读 QA；字段解释见本报告及 `field_dictionary.md`。

首轮只读尝试因 APDL `*VWRITE` 数组自动展开而产生 1680 行长表，被在线 QA 拒绝并完整归档到 `attempt1_rejected_vectorwrite`，未用于任何正式统计。正式输入改为标量写出后重新运行，最终严格命中 80/480 行结构。
