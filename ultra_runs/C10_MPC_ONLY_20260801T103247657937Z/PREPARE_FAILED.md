# C10 准备失败标记

该目录是本轮生成过程中的未封存尝试，因 `input_hash_audit.csv` 首版字段集合不一致而在写 QA 表时停止。

- MAPDL 未启动。
- 本目录没有完整 `manifest.json`、`C10_status.json` 或 `artifact_hashes.sha256`，不得用于计算或审阅结论。
- 已修正生成器并重新生成完整包：`../C10_MPC_ONLY_20260801T105906480975Z/`。
- 保留本目录仅为失败谱系，不代表有效候选。
