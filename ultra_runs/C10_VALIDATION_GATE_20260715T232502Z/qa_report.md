# C10 验证门禁审计报告

## 最终判定

本门禁的正式状态是 `VALIDATION_GATE_MISSING`，启动决策是 `REJECTED_TO_LAUNCH`。本状态不是 `SOURCE_BLOCKED`：现有 U00 明细足以完成 5078 条逻辑连接到 8202 个 MPC184 的静态映射审计，但没有满足 C10 启动所需的罚因子、全桥收敛、矩阵和静力验证证据，而且当前 U00/C10 候选已污染 S10 单变量边界。

本次只读取既有文件并生成审计文档；未启动、排队或调用 MAPDL，未修改生成器，也未生成新的 C10 include。

## 5078 映射闭合

| 项目 | 数量 | 判定 |
|---|---:|---|
| UXYZ 逻辑连接 | 3124 | 每条对应 2 个 MPC184 |
| ALL 逻辑连接 | 1954 | 每条对应 1 个 MPC184 |
| 逻辑连接总数 | 5078 | `3124 + 1954` 闭合 |
| MPC184 单元总数 | 8202 | `3124×2 + 1954×1` 闭合 |
| 唯一 MPC184 单元号 | 8202 | 无重复 |
| 旧 CERIG→MPC184 稳定字段配对 | 5078/5078 | `dof_label↔source_semantics`、system、assembly_name、reason 逐行一致 |
| 映射错配 | 0 | 稳定字段配对、语义、主单元号、偏置单元号和总数均闭合 |

5078 行原始明细不复制。旧 CERIG 明细引用 `D:\张靖皋大桥\03_猫道动力分析\附件2-3全模态精确对齐_V2.0\builder\generated\generated_constraints.csv`，SHA-256 为 `4533e7aa074b18d3fd9e38e899017bc87ffc9aafe27da2bcb5e76330ed6b1da8`；MPC184 明细引用 `D:\张靖皋大桥\03_猫道动力分析\附件2-3全模态精确对齐_V2.0\ultra_runs\U00_SOURCE_GATE_20260715T024455Z\regenerated_source_chain\builder_generated\generated_rigid_connections.csv`，SHA-256 为 `866788b40273e0451a8b2d484248ce9351205afccaa392354a280e063942f3cb`。因 MPC 再生会插入偏置节点，节点号不是稳定 join 键，本审计使用语义、系统、组件名和原因四组稳定字段按行配对。

## 缺失的验证门禁

| 门禁 | 要求 | 当前证据 | 判定 |
|---|---|---|---|
| 罚因子扫描 | `1E9、5E9、1E10、5E10、1E11 N/mm` 五档，每档六个单位荷载/力矩工况，共 30 个档位-工况对；目标三平移滑移 `<=1E-5 mm` | 无正式 C10 验证包 | `MISSING` |
| 全桥相邻档收敛 | 目标物理模态频率相邻档变化 `<0.05%` | 无全桥相邻档结果与同模态配对 | `MISSING` |
| 矩阵门禁 | 相对旧基线的矩阵系数比恶化必须 `<10×` | 历史日志只回显系数比 `>1E8`，没有量化相对旧基线的恶化倍数 | `MISSING` |
| 全桥静力门禁 | MAPDL ERROR、pivot、LS1/LS2、能量、反力、质量、LINK180、拓扑和模态导出全部通过 | 未执行 C10 全桥静力求解 | `MISSING` |

静力硬门禁的完整阈值已逐项写入 `status.json`。任一项缺失都不允许用映射闭合替代。

## U00 相对 S10 的边界污染

正式 S10 边界继承的 A30/A10 截面 61 前六个 ASEC 字段为：

`SECDATA,4997.5,9830899.73958,0,28164706.4583,66066802083.3,176798.958333`

U00/C10 候选截面 61 的对应字段为：

`SECDATA,4997.5,28164706.4583,0,9830899.73958,66066802083.3,176798.958333`

第 2 与第 4 个属性值发生对调，即 Iyy/Izz 对调。该候选不是“从正式封板 S10 派生的纯连接差分”，因此即使映射为 0 错配，也必须硬拒绝启动。

## 历史 5E10 负证据

历史 `modal_20260714_0221_penalty5e10` 主 OUT 在部分子步已经收敛、后续存在未收敛状态时，于第 983456 行记录 ERROR，并明确说明作业因 ABT file 的用户请求而终止；LS1 未完成。该记录只能证明该 Run 没有形成 PASS，也不能作为罚因子收敛证据；它不支持 `5E10 N/mm` 与发散之间的因果结论。日志另有 “Coefficient ratio exceeds 1.0e8” 回显，但没有同口径旧基线值，故既不能量化相对基线恶化倍数，也不能断言已超过或满足 `<10×` 门禁。直接证据为 `runs\modal_20260714_0221_penalty5e10\solve\a23v2_20260714_0221.out`。

## 图纸源存在但尚未集成

下游图纸源 `D:\张靖皋大桥\01_设计资料与规范\00张靖皋长江大桥南航道桥猫道图纸1225.pdf` 已存在，SHA-256 为 `8df26c6b04f652b952e6cdf9575f34e76c8d014cbb61edf0b846c1c75d8bc113`。但 5078 行映射明细没有图号字段，检索到的 MD 图号引用为 0 行，因此当前连接台账尚未完成图纸可追溯集成；该缺口继续隔离到下游连接/铰链封板，不得反向解释为 C10 已验证。

## 启动结论

C10 只能在以下条件同时满足后重新送审：从正式封板 S10 派生纯连接差分；消除 SEC61 边界污染；完成五档×六工况；证明全桥相邻档 `<0.05%`；证明矩阵比恶化 `<10×`；通过全桥静力硬门禁。当前决策保持 `REJECTED_TO_LAUNCH`。
