# 工件账本范围说明

`artifact_hashes_prelaunch.sha256` 是准备完成、求解尚未启动时的原始 25 项冻结账本。根状态、清单和结果包后来按真实运行结果更新，因此预启动账本中的同名文件摘要只代表启动前版本。`artifact_hashes.sha256` 在受控终止后重建并覆盖当前目录全部文件（排除自身），用于验证当前封存证据。随后采用的 NTFS 压缩只改变磁盘存储方式，不改变路径、内容或 SHA-256。
