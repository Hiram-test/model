# C10 恒总荷载位置迁移 5% 步长终止结果

状态：`ABORTED_BY_CONTROLLER_AFTER_LS2_FIRST_5_PERCENT_MIGRATION_DIVERGED`。本运行没有得到完整静力结果，也没有启动模态。

- LS1 的 beta=1 旧荷载位置基态完整收敛：四项准则在第 4 次平衡迭代全部通过。
- LS1 最终力残差为 `0.7009 N`，力矩残差为 `10.78 N·mm`；方程数恒为 `1,234,834`。
- 最小主元为正值 `25.3126539`，没有 small、zero 或 negative pivot；连接奇异没有复发。
- LS2 首个目标为从 beta=1 移到 beta=0.95，即迁移 5% 的二期重力位置。
- LS2 第 1 次迭代初始力残差仅 `5.224 kN`，但位移修正为 `8.899 mm`；第 2 次迭代跳到力残差 `645.4 MN`、力矩残差 `11.86 GN·mm`、位移修正 `-75.00 mm`。
- 下一次残差虽回落至 `49.47 MN`，仍比力准则高约一千倍；该首段步长不具稳定收敛趋势。
- 已确认包装进程 PID 42584 和实际求解器 PID 54524 的完整命令行都只指向本唯一运行与作业后停止两者，没有误杀其他任务。
- 结论：恒总荷载位置修复方向有效，beta=1 已闭合；失败范围被缩小为 LS2 首个 5% 位置迁移过粗。
- 下一步：新建唯一运行，把首段迁移细化十倍至 0.5%，保持修正向量、总荷载、四项 CNVTOL 和单层 TYPE72 拓扑不变。

