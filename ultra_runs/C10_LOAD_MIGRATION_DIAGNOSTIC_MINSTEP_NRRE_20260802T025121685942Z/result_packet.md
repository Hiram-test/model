# C10 首个 0.05% 迁移 NRRE 诊断准备结果

状态：`STATIC_DIAGNOSTIC_PREPARED`；ANSYS 未启动。

- 完整 LS1：beta=1、TIME=1、单子步、自动步长关闭，并独立保存 `cw_C10mnr_0802t025121685942_d1_l1.db`。
- 局部 LS2：beta=1→0.9995、TIME=1→1.0000005、单个固定子步、自动步长关闭。
- 残差输出：`NLDIAG,NRRE,ON,50`，预期 `cw_C10mnr_0802t025121685942_d1.nrxxx`；不启用 EFLG。
- 恒总荷载：15,071 个修正节点，渲染后修正力合计 2.08526E-10 N。
- 连接基线：5,078 个单层 TYPE72，辅助节点=0、TYPE73=0；微验证 12/12 通过。
- 运行代码：专用启动器与专用监控器已冻结并纳入准备账本；硬事件只允许官方 ABT，禁止强制结束进程。
- 禁止范围：不继续 beta=0 全路径，不运行模态，不形成完整静力或生产结论。
