# D10 下压系统草稿（待接到收敛的 C20）

相对 S10 `apply_mct_downpull_equivalent_xlong.inp` 的单变量：

- 删除 4 根等效 LINK + 12 条把 16 根底索 UX/UY/UZ 绑死的 CP。
- 每组 8 根 LINK180，面积为原等效面积的 1/8，初应变不变，组总 EA 与 ISTN 与 S10 相同。
- 8 根直接连到 16 根底索中的隔根节点（耳板取点），方向随索位 Y 散开。
- 节点 90000/90002/90004/90006 仍为塔侧固定点。
- 单元号 400000–400031（原 400000–400003 四处等效被替换）。

拓扑变化（接到 C20 SPRING 时再计入 COMBIN14）：

- 节点：仍为 109086（不新增）
- 单元：172994 - 4 + 32 = 173022（若 C20 已加 568 个 COMBIN14，则为 173590）
- CP：12 → 0（下压相关）

不要与正在跑的 C20 SPRING 抢 ANSYS。C20 静力+模态通过后再复制其 solver 并替换本 include。
