# 恒总荷载迁移准备失败记录

状态：`PREPARATION_FAILED_BEFORE_MAIN_INPUT_COMMIT`。本目录没有启动 MAPDL，也没有生成静力或模态结果。

准备期命令计数把说明文字中的 `KBC` 名称误计为可执行命令，因此在主输入提交前按门禁停止。已把复制来的父全模态入口从 `solver` 隔离到 `input_snapshot`，本目录没有可授权启动的主控、清单或启动命令。该目录仅保留失败证据，禁止执行、续跑或复用。
