# S10_SECTION_SHEAR 启动合法性只读审计

## 结论

总体判定为 `RESOURCE_BLOCKED`。源证、父线、单变量和全桥输入的只读干构建均已通过，因此源侧状态为 `PASS_TO_PREPARE`；但本次快照的可用物理内存和 D 盘空间同时低于全桥启动硬门槛，当前禁止启动 MAPDL。本审计未生成正式 S10 run，未排队、未启动或调用 MAPDL，也未修改 A30 或 C10。

## 审计基准

审计逐项对照 Ultra 任务书第 8、13、14 节。任务书 SHA-256、最终 A30、U01、U02、A10/A20 父源以及准备脚本的逐文件摘要见 `source_hashes.sha256`；本包自身闭合见 `artifact_hashes.sha256`。

`status.json` 中 `source_gate_status` 表示源证是否足以进入 prepare-only 阶段；`resource_gate_status` 表示当前时点是否允许真正启动全桥求解；`decision` 采用更严格的总体结论。JSON 与哈希账本格式不支持注释，字段和单位由本报告集中说明。

## 第 8 节：六类截面源证

最终 U01 为 `U01_UNIT_TESTS_20260715T225028311569Z`，8/8 通过，截面案例为六类乘两个全局方向，共 12 对原生截面/ASEC 短梁直接比较。U01 的 182 项非自引用产物与账本集合、逐文件 SHA-256 全部闭合。

| 门禁 | 实测最大值 | 阈值 | 结论 |
|---|---:|---:|---|
| ASEC 的 A/Iyy/Izz/J 对任务书冻结目标相对误差 | 3.5434996739929223E-16 | <1.0E-3 | PASS |
| native-vs-ASEC 总挠度相对差 | 4.1308763442944064E-8 | <5.0E-3 | PASS |
| native-vs-ASEC 扣弯后剪切挠度相对差 | 1.5668441003048359E-8 | <5.0E-3 | PASS |
| ASEC 对解析式挠度相对误差 | 1.4042078936210875E-5 | <5.0E-3 | PASS |
| 剪切参数方向映射相对误差 | 4.428430504115966E-16 | <5.0E-3 | PASS |
| MAPDL section warning | 0 | 0 | PASS |

H175 原生截面相对 ASEC 采用绕梁轴 90° 的物理方向映射；其余五类保持 K 指向 +Z。原生截面只承担剪切柔度参考角色：native 自动计算的 J 对冻结目标最大差为 7.087934%，不能据此替换生产 J。S10 保留 ASEC 前六字段 A/Iyy/Iyz/Izz/Iw/J，只追加零偏置、TKz/TKy 和 U01-v261 双向剪切因子。

## 第 13 节：A30 父线与单变量性

权威父包固定为 `A30_ALL_AXES_20260715T230922215766Z`，状态为 `COMPLETED_BY_INPUT_IDENTITY_WITH_A10_RESULTS`，QA 为 `PASS_RESULT_REUSE_WITH_DOCUMENTED_LIMITATIONS`，下一步明确为 `S10_SECTION_SHEAR`。A30 的 9 项非自引用产物集合和逐文件哈希全部闭合。

A20 证明 RHS50×30 的 2,898 根梁无需物理改动，因此 A30 的实际可执行输入与已求解 A10 字节相同。S10 合法地以最终 A30 作为物理父线、以 A10 的 11 项实际求解输入作为字节父源：10 项依赖逐字不变，仅 `apply_finite_gates_and_passages_v2.inp` 发生一个物理变化族。

只读内存变换命中 SEC61..66 恰好六条 `SECDATA`。原 include SHA-256 为 `68d9dc...aa3c`，干变换后为 `72012e...7330`；剥离新增八字段和说明后重新得到原哈希。A、Iyy、Iyz、Izz、Iw、J、材料、密度、轴节点、网格、质量、荷载、初始索力、CERIG/CP 和 KEYOPT 均未改变。六组件覆盖目标为 17,679 根有限梁。

## 第 14 节：全桥输入与数值门禁

准备脚本以当前 `ultra_b00_prepare.py` 生成从头 LS1→LS2 保持→80 阶无频带 LANB 主流程。干构建确认：11 项依赖顺序为 1..11，LS1/LS2 收敛、LS1 端点和全历程峰值 STEN/SENE、LS2 STEN/SENE、质量、支反力、拓扑/TYPE 数量和严格 80 阶结果闭合门禁均存在；`MXPAND` 启用单元计算，并为六类组件生成 80 阶 SENE 输出。83 个附加能量块全部剥离后逐字等于 S10 控制基线。

`U02_LS1_HISTORY_MICROQA_20260715T225026Z` 已在 ANSYS 2026 R1/v261 上用 6 个静力结果集验证 `SET,FIRST/NEXT`、5 个 LS1 子步导出、LS2 单端点和峰值计算控制链；19 项产物闭合。U02 仅证明控制语法，不冒充未来 S10 全桥的物理结果。MAPDL ERROR、pivot、LINK180 非正轴力以及真实结果文件数仍必须在未来求解后判定，当前审计不声称这些运行后指标已产生。

## 准备脚本修订

审计发现原脚本虽定义了 `validate_u01_section_evidence()` 和 `validate_history_microqa()`，但主路径没有调用，且 U01 manifest/四份原始 CSV 的固定哈希常量未参与拒绝逻辑。现已完成最小修订：切换到最终 A30 固定身份，逐项核验所有已声明 U01 固定哈希，执行 U01 182 项和 A30 9 项产物闭合，接入十二对截面复算与 U02 历程控制链，并把相关原始证据纳入未来 S10 lineage。脚本由 `2042f7...652c0` 更新为 `074b3e...a17f`；AST 解析、全部父门禁和只读内存干构建均通过。

## 资源门禁

资源快照时间为 2026-07-15T23:17:08.917397Z：

- 可用物理内存 4,451,053,568 byte（4.145367 GiB），低于 8 GiB 门槛，缺口 4,138,881,024 byte（3.854633 GiB）。
- D 盘可用 30,100,930,560 byte（28.033676 GiB），低于 32 GiB 门槛，缺口 4,258,807,808 byte（3.966324 GiB）。
- 未记录用户内存门禁覆盖；即使只覆盖内存，磁盘门禁仍不可覆盖。

因此当前总体结论只能是 `RESOURCE_BLOCKED`。释放资源并重新快照前，不得启动 MAPDL。源侧已达到 `PASS_TO_PREPARE`，但本审计严格停在只读干构建，没有创建 S10 正式准备包。

## 下游边界提示

独立边界审计发现现有 U00/C10 候选 include 相对 A30 将 SEC61 的 Iyy/Izz 对调，不是“基于封板 S10 的纯连接差分”。该问题不污染本轮 S10 源；但当前 C10 候选不得启动，后续 C10 必须从正式封板 S10 输入重新派生。本审计未修改 C10。
