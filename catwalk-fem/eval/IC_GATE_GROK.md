# Grok 自评：初应力词法门

依据：本机对两份 `.inp` 的只读扫描 + `sha256sum`。不信任写入器 JSON 自报。未改任何 `.inp`。

1. 冻结 `82548e6a` 运行前/后哈希相同，两行仍是 `E_FLOOR_ROPE` / `E_PORTAL_ROPE` + 单轴。
2. 新主 `41fb3222`：204208/204208 行 `ccx221`（数字单元号、1–8 积分点、六分量），0 行 ELSET+单轴。
3. 写入器 `write_inp.py` 只走 `format_pk2_stress_rows`，不再写 `E_FLOOR_ROPE, σ`。
4. 142 榀（不是榌）。坐标、分锚、21 通道不在这道门里重开。

**判定：词法门过。交独立回读。82548e6a 未动。**
