# MCT geometry inverse form-finding

This directory uses the verified MCT formed coordinates, element topology, material self-weight, stage-two nodal loads, and support topology. Stored cable forces are excluded from the inverse solve and loaded only afterward for elementwise verification.
