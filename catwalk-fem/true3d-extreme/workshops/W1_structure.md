# 工坊 1 · 结构基本计算

性质：本轮求解回填。数字来自 `artifacts/`。不是科学结论。

## 1.1 模型与契约

- 源：S10 V2.0 APDL。deck sha `f089dee33377`，COARSEN=4。
- 节点 5422，单元 7477 B31。8 条并索线（每幅内/外 × 承重/门架）。
- 门架 142 榀。通道：R5 要求 21 品。本轮求解 deck 曾写出 63 个 x 站（21 簇 × 3，深度约 1.4 m）。建模器现按 5 m 间隙聚类为 21 根等效梁；须重跑 S3 才改网格。
- 质量台账 4108.466 t，对 S10 4108.467 t，相对差 2.4×10⁻⁷。G-P2 PASS。

## 1.2 静力与模态

| 门 | 结果 |
|---|---|
| G-P1 | PASS，44 索线，F/g ≡ MASS21 = 963.811381 t |
| G-P2 | PASS |
| UY | PASS，84/5422，frac 0.0155 |
| G-P3 | PASS，3 次牛顿，152 s |
| G-P4 | **FAIL**。4 个 ~2×10⁻⁴ Hz 残差刚体仍在前 100 阶（已从模态基剔除）；末残差/自重 = 1.42×10⁻⁶ > 1×10⁻⁶。`conclusion_allowed=false`，图谱只作对照。 |

首个结构模态 0.03982 Hz（LS1 向）。锁定十四行配对见 `true3d_table41_pairing.csv`。

LS2 锁定配对落到 0.2085 Hz（相对附件 0.1087 为 +91.8%，频率比 ≈ 1.92）。这是**族内主跨 L+S 的第 2 个**，半波指纹是 5，不是 3。中间两个 L+S（0.138、0.182 Hz）主跨能量 < 0.65，按锁定规则进边跨池，禁止用半波 MAC 改配。LS1 本身只有 +9.1%。不要把 1.92 读成“全模型刚度/质量错了一倍”。

T 族只引用三栈括弧，不写复现/一致。TA1 本 deck +18.9%（0.1184 vs 0.0996），符号与双 MCT 焊接端括弧（−19%~−15%）不同，记为三维拓扑的对照事实，不下结论。

COARSEN=2 收敛档：〔待填:未跑〕。

## 1.3 设计风档抖振

苏通锚点 U10=38.9 m/s，Iu=0.11，U_deck=47.5 m/s。气动已从附件表 3-1 回填（每带 Cd*D=0.395 m，Cl'B=0.559 m），不再是占位 0.9。

跨最大 RMS：L 7.71 m，V 0.91 m，Tcw 0.033 rad。A4 三点对照用附件表 5-1（V10=30.1，竖向含静力偏置）。对照只写形状与量级，不写百分比符合度。
