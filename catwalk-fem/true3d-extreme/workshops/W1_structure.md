# 工坊 1 · 结构基本计算

性质：本轮求解回填。数字来自 `artifacts/`。不是科学结论。

## 1.1 模型与契约

- 源：S10 V2.0 APDL。deck sha `5ebc64fae00a`，COARSEN=4。
- 节点 5018，单元 6867 B31。8 条并索线（每幅内/外 × 承重/门架）。
- 门架 142 榀。通道：R5 已落地——63 个 x 站（21 簇 × 3）按 5 m 间隙聚类为 **21 根等效梁**，本轮 deck 即此网格；S10 id ≥ 100000 经 `deck_id_from_s10` 重映射，不再撞 `100000*(g+1)+k` 编号。
- 质量台账 4108.466 t，对 S10 4108.467 t，相对差 2.4×10⁻⁷。G-P2 PASS。

## 1.2 静力与模态

| 门 | 结果 |
|---|---|
| G-P1 | PASS，44 索线，F/g ≡ MASS21 = 963.811381 t |
| G-P2 | PASS |
| UY | PASS，84/5018，frac 0.0167 |
| G-P3 | PASS，3 次牛顿，152 s |
| G-P4 | **FAIL**。4 个 ~2×10⁻⁴ Hz 残差刚体仍在前 100 阶（已从模态基剔除）；末残差/自重 = 4.85×10⁻⁶ > 1×10⁻⁶（末牛顿残差 195 N）。`conclusion_allowed=false`，图谱只作对照。 |

首个结构模态 0.03904 Hz（LS1 向）。锁定十四行配对见 `true3d_table41_pairing.csv`：14 行 MAE 7.1%（63 x 站旧 deck 为 15.3%）。

LS2 锁定配对本轮落到 0.1222 Hz（相对附件 0.1087 为 +12.5%，半波指纹 3）。63 x 站旧 deck 上同一锁定规则曾落到 0.2085 Hz（比 1.92，半波指纹 5）；R5 聚成 21 根等效梁后，族内第 2 个主跨 L+S 回到 3 半波。对照事实，不下结论。

T 族只引用三栈括弧，不写复现/一致。括弧的**下沿不是平面模型专属**：仓库里已经有 S10 V2.0 ANSYS 全三维（109086 节点，f99 链 S10/C20/D10/E10），TA1 钉在 0.0733 Hz（−26.4%，与 2f* 重合）。本 deck TA1 +6.5%（0.10604 vs 0.0996）**高于**那条已锁的三维地板，不是「三维拓扑把附件救回来了」。

横向惯量审计（`artifacts/lateral_inertia_audit.json`，引理 A）：本 deck 质量/张力横向 rms 都是 ~21.5 m，预测 TA1/VA1 ≈ 1.00；实算 1.46（附件比 1.42）。通道抗弯降到 1/100（拓扑仍在）TA1 只掉 2.65%；拆除/铰接/I=1e-4 静力发散。抬升路径尚未隔离，对照事实，不下结论。

COARSEN=2 附加档已跑（独立 job，不覆盖 C4 主档）。相对 C4：TA1 −7.49%，TS1 −1.61%，LS1 −1.02%，VA1 −0.36%（`artifacts/coarsen2_shift.json`）。TA1 对 R2 粗化最敏感，其 +6.5% 对照差与粗化误差同量级，只作记录。

## 1.3 设计风档抖振

苏通锚点 U10=38.9 m/s，Iu=0.11，U_deck=47.5 m/s。气动已从附件表 3-1 回填（每带 Cd*D=0.395 m，Cl'B=0.559 m），不再是占位 0.9。

跨最大 RMS：L 7.71 m，V 0.80 m，Tcw 0.028 rad。A4 三点对照用附件表 5-1（V10=30.1，竖向含静力偏置）。对照只写形状与量级，不写百分比符合度。
