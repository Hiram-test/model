# E4 high-master toy deck

This directory writes a two-line T3D2 + `TYPE=MASS` + `*FREQUENCY` toy deck.

It is **not** Track B blind E4. `HUMAN_WORKFLOW_RECONSTRUCTION.md` requires E4
pilot heights to come from drawing portal geometry and forbids inverse-fit
equivalent heights. The numbers here are copied from Track A
`forensic_fit.json`:

- shared height 5.607 m
- TA1 extra height 3.461 m
- `dM = 13 × 10.1346 t`

`frequency_reproduced=false`. `back_tuned=true`. Not C3-UB-FT14. Not true3d.
Do not write attach TA1 0.0996 / 复现 / 一致 from a green Actions run.
