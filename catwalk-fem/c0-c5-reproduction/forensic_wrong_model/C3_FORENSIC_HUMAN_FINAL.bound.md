# C3 forensic ROTX/LINK forward probes — evidence bound

`frequency_reproduced=false`. `human_apdl=false`. `not_attach_ta1=true`. `not_undisclosed_human_apdl=true`.

New branch `agent/c3-forensic-human-final-20260831` from `d372295`. Cases are target-blind ROTX and LINK/high-pilot hypotheses on frozen C3 `667c5047`. The 9.08 m uniform height is a template number, not attach TA1 and not the Track A inverse 9.069 m. Receipt already says the unavailable original APDL cannot be proved.

Actions `33335726221` on `73699ec` (v3) failed while reconstructing the archived builder (`base64 -d` of `build_human_error_variants.py.gz.b64`). No Job finished, no equation count, no frequencies. Do not write 复现, 一致, or 原始人工过程.
