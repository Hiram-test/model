--- upstream/gen3dnor.f
+++ joint-coordinates/gen3dnor.f
@@ -35,6 +35,7 @@
 !     6: rot_about_z
 !     
       implicit none
+      integer, allocatable :: requestedknots(:)
 !     
       logical fixed,composite,beam
 !     
@@ -73,6 +74,22 @@
 !     calculating the normals in nodes belonging to shells/beams
 !     
       nkold=nk
+      allocate(requestedknots(nkold)) ! Input joint
+      requestedknots=0 ! Input joint
+      do j=1,nmpc ! Input joint
+         index=ipompc(j) ! Input joint
+         do while(index.ne.0) ! Input joint
+            node=nodempc(1,index) ! Input joint
+            if((node.gt.0).and.(node.le.nkold)) then ! Input joint
+               if((nodempc(2,index).gt.3).or. ! Input joint
+     &              (labmpc(j)(1:5).eq.'RIGID')) then ! Input joint
+                  requestedknots(node)=1 ! Input joint
+               endif ! Input joint
+            endif ! Input joint
+            index=nodempc(3,index) ! Input joint
+         enddo ! Input joint
+      enddo ! Input joint
+
 !     
       do i=1,nkold
         ndepnodes=0
@@ -827,6 +844,8 @@
 !     
 !     generate rigid MPC's if necessary
 !     
+        if((requestedknots(i).eq.1).and.(nexp.eq.1).and.
+     &       (nnor.gt.0)) nexp=2
         if(nexp.gt.1) then
           irefnode=i
 !     
