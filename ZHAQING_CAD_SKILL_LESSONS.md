# 扎青吊桥 CAD 自动化案例复盘：Skill 调用、装配一致性与质量门

## 1. 文档目的

本文记录一次使用 Bridge FEM Skill Suite 对 82 m 扎青吊桥进行三维 CAD 自动化试建的完整经验。重点不是展示模型成果，而是复盘为什么最初虽然使用了 N01–N07、G0–G6 等名称，却没有真正执行 Skill 契约；为什么后来错误地自行建立“门禁”；以及如何把几何内核检查、装配语义检查和原生 Gate 重新统一起来。

本案例当前只讨论 CAD 几何和有限元前处理准备，不包含正式有限元求解、结构安全结论、施工放样或加工下料。

## 2. 核心结论

1. **调用 Skill 不等于引用 Skill 名称。** 只有读取完整输入契约、生成规定工件、满足上游依赖并由原生 Gate 判定，才算真正执行 Skill。
2. **零穿透、零重复、零孤立只是最低拓扑要求。** 它们不能证明构件安装在工程上正确的位置，也不能证明传力路径正确。
3. **自定义检查器可以实现 Gate，但不能自行定义 Gate。** FreeCAD/OpenCASCADE 检查程序应把结果写入原生工件，不得另起正式门禁编号替代 G3–G6。
4. **装配正确性横跨 N04–N07。** 图纸坐标配准、构件装配图、传力路径、有限元抽象接口和拓扑几何必须共同闭环。
5. **工件 schema 通过不等于工程语义通过。** 内容不完整但格式合法的 JSON 不能成为放行依据。
6. **无效模型不应先做“论文图”。** 模型图必须服务于模型定义、网格、坐标、边界和连接说明，而不是先做装饰性渲染。

## 3. 事件经过

### 3.1 第一阶段：先生成模型，再补 Skill 证据

初版 CAD-001 创建了桥面、纵横梁、索塔、基础、锚碇、主缆、吊杆和栏杆等实体，并导出了 FreeCAD、STEP 和预览图。

当时的工作顺序存在根本问题：先追求“把桥建出来”，随后再补任务包、工件和 Gate 台账。这样生成的文件虽然能打开，却没有在建模前落实 N04–N07 对坐标、装配、传力路径、接口和回投验证的要求。

严格复核后发现：

- 376 个语义构件实体之外还保留了同位的完整装配副本；
- 存在 450 对非设计意图的实体穿透；
- 多类构件通过体积搭接表达连接；
- 外观完整被错误地当成几何一致性通过。

CAD-001 因此只能作为概念建模和问题暴露版本。

### 3.2 第二阶段：针对穿透进行几何修正

CAD-002 新建独立运行目录，没有覆盖 CAD-001。主要修正包括：

- 删除保存前的完整装配副本；
- 横梁在纵梁位置分段修剪；
- 塔柱、塔梁和桥面系改为面接触；
- 主缆分段融合为连续实体；
- 主缆在锚碇处切除；
- 索鞍按主缆开槽；
- 吊杆上端按主缆表面切除；
- 栏杆立柱止于顶索底面。

OpenCASCADE 检查得到零重复、零孤立、零实体穿透和零自相交。此时错误地把这些结果解释为 N07/G6 已经通过。

### 3.3 第三阶段：用户指出装配位置仍然错误

进一步复核发现，CAD-002 虽然解决了实体穿透，却没有证明装配位置正确。例如：

- 吊杆下端接触桥面板顶面，而不是横梁端部、加劲梁或经图纸确认的吊点接口；
- 风缆、抗风吊杆和风缆锚碇未进入模型，横向传力路径不完整；
- 锚点标高、锚碇外形、塔柱截面、承台和桩基尺寸属于展示假定；
- 主缆采用指定抛物线，并非找形后的平衡线形；
- 主缆索面、索鞍、塔柱和桥面横向位置没有真实图纸 overlay 支持；
- 支承、索夹、吊杆连接件、锚固接口及偏心关系没有建立。

该阶段证明：**几何接触图不能替代工程装配图，连通分量不能替代传力路径。**

## 4. 为什么最初没有真正调用成功 Skill

### 4.1 把 Skill 当成说明文档，而不是执行契约

初始实现使用了节点和 Gate 名称，但没有严格执行“冻结输入—生成规定工件—验证上游 Gate—运行当前节点—写入原生台账”的顺序。

真正调用 Skill 至少应满足：

- 读取当前节点和相邻节点的完整 `SKILL.md`；
- 检查输入工件是否齐全并绑定哈希；
- 生成该节点列出的全部输出工件；
- 验证工程内容，而不只验证 JSON schema；
- 只更新原 Skill 定义的 Gate；
- 上游 Gate 未通过时阻断下游节点。

### 4.2 没有落实上游依赖

CAD-002 的工件审计显示：

- N04 规定 8 份工件，只生成了 `evidence_graph.json`，缺少 7 份；
- N05 规定 7 份工件，只生成了 `component_inventory.json`，缺少 6 份；
- N06 规定 6 份工件，只生成了 `abstraction_decisions.json`，缺少 5 份。

共缺少 18 份上游必需工件。特别是以下关键工件不存在：

- `coordinate_transform_register.json`；
- `round_trip_overlay_report.json`；
- `assembly_graph.json`；
- `load_path_graph.json`；
- `orphan_evidence_audit.json`；
- `abstraction_validation_plan.json`；
- `part_to_component_to_representation_map.json`。

因此 G3、G4、G5 不具备放行条件，N07/G6 也不具备正式执行资格。

### 4.3 把局部统计冒充图纸回投

名为 `geometry_overlay_report.json` 的工件只核对了主跨、桥宽、吊杆数量和主缆数量，没有产生平面、立面、横断面和节点详图的真实叠合图，也没有控制点变换矩阵和残差。

这只能称为全局数量检查，不能称为 Skill 所要求的回投 overlay。

## 5. 为什么会自行建立“门禁”

用户指出重影、悬空和穿透后，为快速量化问题，增加了以下检查：

- 拓扑哈希与重复实体检测；
- OpenCASCADE 公共体积检查；
- 构件最短距离检查；
- 实体接触图和连通分量检查；
- STEP 回读及包围盒检查。

这些程序本身有价值，错误在于一度把自定义编号或自定义报告当成正式 Gate。

正确边界应当是：

- 自定义代码是 **Gate adapter / diagnostic implementation**；
- 原生 G3–G6 才是正式质量门；
- 自定义结果必须写入原生输出工件；
- 新增检查项可作为补充证据，但不得自行改变 Gate 定义；
- 如需增强 Skill，应通过版本化变更修改 Skill 契约和 schema，而不是在单次项目中临时发明门禁。

## 6. 原 Skill 对装配位置的实际要求

### 6.1 N04：图纸配准与协调

N04 要求建立全局桥梁坐标、视图坐标、CAD 块局部坐标和构件局部坐标，保存变换矩阵、控制点、残差和适用范围，并把接受坐标和轮廓回投原图。

这一步解决“构件在图纸的哪里”。若 N04 未完成，后续模型位置只能是未验证假定。

### 6.2 N05：结构语义、装配图和传力路径

N05 要求使用 `part_of`、`connected_to`、`supported_by`、`anchored_to`、`suspended_from` 等关系建立装配图，同时建立竖向、横向、纵向、扭转、索力、锚固和基础传力路径。

这一步解决“构件应该和谁连接、怎样传力”。仅检测两个实体是否接触，无法替代 N05。

### 6.3 N06：CAD 到 FEM 的抽象接口

N06 要求为显式或等效构件定义参考线或中面、连接接口、偏心、局部坐标、刚臂、MPC、支承、锚固、初始状态和验证项目。

这一步解决“真实装配如何转换为有限元物理关系”。CAD 实体相切并不等于有限元自由度已经正确连接。

### 6.4 N07：拓扑与几何生成

N07 要求检查关键坐标、标高、支承、锚固、索鞍、构件交点、设计几何与分析几何差异，并执行图纸回投。零长度、重复、孤立、误连、自交和负体积只是其中一部分。

这一步解决“有限元几何是否同时满足尺寸、接口、拓扑和证据要求”。

## 7. 正确的 Gate 判定

对本案例的严格复核结论应为：

| Gate | 正确状态 | 原因 |
|---|---|---|
| G3 | `BLOCKED` | 坐标变换、尺寸登记和真实图纸回投缺失 |
| G4 | `BLOCKED` | 装配图、传力路径和 orphan audit 缺失 |
| G5 | `BLOCKED` | representation contract 和验证计划不完整 |
| G6 | `BLOCKED` | 上游未通过，关键装配位置也未获得图纸验证 |

原先的 `PASS_WITH_BOUNDS` 应撤回。`PASS_WITH_BOUNDS` 适用于证据边界明确、工程路径完整且风险被有界表达的情况，不适用于必需工件缺失或传力路径尚未建立的情况。

## 8. 论文图方面的经验

初次所谓“论文图”实际上只是白底、低饱和颜色的 CAD 实体渲染。它存在以下问题：

- 没有有限元节点、线单元、壳网格或实体网格；
- 没有坐标轴、支承符号和边界条件；
- 没有显示连接、偏心和单元类型；
- 没有整体图与关键局部节点图的组合；
- 图片内部包含过大的标题和状态脚注；
- 大量留白，模型信息密度低。

合格的模型论文图应根据论文目的选择内容：

1. **CAD 几何论文图**：正交投影、紧凑裁切、有限配色、必要尺寸和构件编号，配平面/立面/横断面或局部爆炸图。
2. **有限元建模图**：显示节点、网格、单元族、局部坐标、支承、耦合、刚臂和质量等效关系。
3. **结果图**：显示明确物理量、单位、色标、变形倍率、工况和极值位置。

在 G3–G6 未通过前，不应继续制作对外使用的论文模型图，以免把错误装配包装成可信工程成果。

## 9. 下次必须遵守的执行顺序

### 9.1 启动阶段

1. 冻结源图纸、版本和哈希；
2. 完整读取 N00 和本次涉及的 N01–N07；
3. 建立不可变 Run ID；
4. 明确分析用途、允许用途和禁止用途；
5. 为每个 Gate 建立“输入工件齐全性”检查。

### 9.2 N04：真实坐标配准

1. 转换并解析 DWG；
2. 建立平面、立面、横断面和详图坐标变换；
3. 用桥轴、塔位、标高、中心线和公共控制点求解变换；
4. 保存残差；
5. 生成真实 overlay 图片和逐控制点偏差表。

### 9.3 N05：建立装配与传力路径

1. 完整覆盖桥面、索塔、主缆、吊杆、风缆、锚碇、基础、支承和附属构件；
2. 每个 Part 唯一映射；
3. 建立装配父子关系；
4. 建立竖向和横向传力路径；
5. 检查断链和孤立对象。

### 9.4 N06：冻结有限元抽象

1. 定义每个构件唯一处置；
2. 明确中心线、中面、偏心和接口；
3. 明确索单元、梁单元、壳单元或等效质量表达；
4. 规定支承、锚固、耦合和初始状态；
5. 建立验证项目及阈值。

### 9.5 N07：再生成模型

1. 由已批准坐标和装配关系生成几何；
2. 逐实例检查接口，而不是只检查构件组最短距离；
3. 同时检查错误接触、漏接和穿透；
4. 回投平面、立面、横断面和节点详图；
5. 只有原生 G6 满足全部条件才进入后续节点。

## 10. 建议补强 Skill Suite 的机器门禁

为了防止同类错误再次发生，建议在未来版本加入以下可自动执行规则：

1. 当前节点运行前强制检查全部上游必需工件存在且哈希绑定。
2. Gate 台账拒绝未在 Skill Catalog 中登记的正式 Gate ID。
3. `geometry_overlay_report` 必须包含视图、变换矩阵、控制点数量、残差和可访问的 overlay 文件，不允许只有文字名称。
4. N05 缺少 `assembly_graph` 或 `load_path_graph` 时，G4 自动 `BLOCKED`。
5. N06 的每个显式构件必须具有 reference geometry、connection interface、offset 和 validation check。
6. N07 必须逐实例检查 required interface，禁止用构件组的单一最短距离代表全部实例。
7. 接触审计同时区分 `required_contact`、`allowed_clearance`、`forbidden_penetration` 和 `forbidden_contact`。
8. 高敏感展示假定不得用 `PASS_WITH_BOUNDS` 绕过缺失的正式坐标和装配事实。
9. 发布模型图时必须标明模型类型：CAD、FEM geometry、mesh、result，不得混称。

## 11. 有限元计算的下一步

在重新完成 G3–G6 后，建议先建立最小可行有限元模型：主缆、吊杆、风缆、索塔、等效桥面梁系、锚固和支承。计算步骤为：

1. 补齐材料、截面、密度和连接参数；
2. 建立主缆和吊杆找形模型；
3. 求取初始索力和无应力长度；
4. 检查恒载平衡、索力正值和几何残差；
5. 建立三维梁—索整体静力模型；
6. 检查刚体模态、约束、支反力和全局力平衡；
7. 经明确授权后运行 MAPDL；
8. 封存输入、APDL、日志、结果和哈希；
9. 完成独立复核后再增加风、温度、活载和施工阶段。

未执行真实求解时必须标记为 `PREPARED_NOT_EXECUTED`，不得以解析解或展示结果冒充求解器输出。

## 12. 最终经验

本案例最大的教训不是“检查得还不够多”，而是最初没有尊重 Skill 对工程事实和放行权的划分。

- 图纸决定接受坐标和装配事实；
- N04–N06 决定这些事实如何组织和抽象；
- N07 把它们转换成可检查的几何和拓扑；
- OpenCASCADE、FreeCAD、OCR 和求解器只是实现工具；
- 正式放行只能由原 Skill 的 Gate 给出。

以后应坚持：**先完成证据与装配闭环，再生成几何；先验证原生 Gate，再制作展示图；自定义代码服务 Gate，不替代 Gate。**
