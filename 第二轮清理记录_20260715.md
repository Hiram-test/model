# 第二轮试算启动前清理记录

执行时间：2026-07-16 00:00（UTC+8）

## 清理原则

- 保留 `B00_LEGACY_COMPLETE_20260715T111105670409Z` 的完整基线、求解结果、59 阶节点振型、输入快照、日志、哈希和交付包。
- 保留旧试算的 `.inp`、`.out`、`.err`、`.log`、`.md`、`.json`、`.csv` 等输入与审计证据，以便追溯失败原因。
- 仅删除能够由保留输入重建、且已经被 B00 完整基线取代的求解器二进制或临时目录。
- 不清理其他项目、论文、专利、CAD、原始资料或最终交付目录。

## 已批准清理范围

| 范围 | 数量 | 清理前大小 | 原因 |
|---|---:|---:|---|
| `_agent_scratch/quick_modal_penalty_20260713_1948` | 102 个文件 | 9.498 GiB | 已被 B00 完整基线取代的临时求解目录 |
| `_agent_scratch/quick_modal_20260713_1526` | 57 个文件 | 7.334 GiB | 已被 B00 完整基线取代的临时求解目录 |
| `_agent_scratch/quick_modal_split_20260713_1935` | 48 个文件 | 6.389 GiB | 已被 B00 完整基线取代的临时求解目录 |
| `runs/modal_20260714_0132_directlink/solve` 下旧失败试算二进制 | 该目录内可重建的求解器二进制 | 约 6.34 GiB | 该轮因刚体运动失败且没有外部依赖；删除 `.esav/.emat/.full/.db/.rdb/.r001/.rst/.rstp/.mode/.mlv/.ldhi/.DSP`，保留文本证据与输入 |
| `runs/modal_20260714_0221_penalty5e10/solve` 下未被续算依赖的二进制 | `.esav/.emat/.full/.db/.rstp/.mode/.mlv/.DSP` | 约 6.16 GiB | 保留 0627 续算清单依赖的 `.rdb/.ldhi/.r001/.rst` 及主 `.out/.inp` |
| 已被最终通过轮次取代的 U01 目录 | 4 个非空目录和 1 个空目录 | 34.75 MiB | 早期轮次为失败或未完成；保留最终通过的 `U01_UNIT_TESTS_20260715T101428628522Z` |
| `tmp/free_uy_cases23_audit_20260713` | 旧自由 UY 审计临时副本 | 648.05 MiB | 22 个有效文件与正式归档按 SHA-256 一致，独有内容为磁盘不足失败产物与临时解析文件 |

预计释放约 36.35 GiB。实际释放量以删除后的 D 盘可用空间复核为准。

## 明确保留项

- `ultra_runs/B00_LEGACY_COMPLETE_20260715T111105670409Z/` 全部内容。
- `ultra_runs/U00_SOURCE_GATE_20260715T024455Z/`。
- `ultra_runs/U01_UNIT_TESTS_20260715T101428628522Z/`。
- `B00_0-0.35Hz_59阶_修正输入与计算结果_20260715.zip`。
- `runs/` 下所有输入、主输出、配置、状态、审计和说明文件。
- `runs/modal_20260714_0221_penalty5e10/solve` 中供 0627 续算追溯的 `.rdb/.ldhi/.r001/.rst`。
- `runs/modal_20260714_0322_penalty5e10_restart_stab1e4/` 与 `runs/modal_20260714_0627_penalty5e10_continue_stab1e4/` 的全部内容。
- `_agent_scratch/` 中不属于上述三个 quick-modal 目录的内容。

## 删除后复核

删除完成后必须重新统计 D 盘可用空间、确认 B00 目录和最终 U01 目录仍存在，并确认没有 ANSYS/MAPDL 活动进程误用被清理路径。

## 实际执行结果

- 已删除 4 个明确限定的递归临时目录，共计 23.853 GiB；其中包含三组 quick-modal 和一组重复自由 UY 审计副本。
- 已删除旧失败/被替代试算中的 34 个可重建求解器二进制，共计 12.497 GiB。
- 已删除 5 个早期 U01 目录，共计 34.75 MiB。
- 删除期间存在的 `ansyslmd` 是常驻许可证服务，不是 MAPDL 求解进程；未发现 `ANSYS261/mapdl/mpiexec/hydra` 活动作业。
- D 盘可用空间由约 13.18 GiB 提升至 48.97 GiB；受同时发生的其他工作区写入影响，卷级实测净增加 35.786 GiB。
- B00 必需的 `RSTP/MODE/FULL/ESAV`、59 阶交付属性文件均存在，缺失数为 0。
- 0221 续算追溯所需的 `.rdb/.ldhi/.r001/.rst` 均存在，缺失数为 0。
- 最终通过的 `U01_UNIT_TESTS_20260715T101428628522Z` 仍完整存在。
