# 猫道比对章 — 实验与结果

本目录是 Experiment Setup & Results 审计，不是已求解结果。

- `results-audit-20260825.md`：Skill 映射、应跑设置、检索台账、14 QoI 表、中文草稿、向 Methodology / ccx·inp 的缺口、自评。
- `qoi-table.json`：14 行机器表。参考 = 附件 2-3 表 4-1；本研究 = `NA-NO-SOLVE`；判定 = `insufficient`。
- `audit-search-log.json`：检索对象、位置、哈希。

`main` `920a94b` 没有猫道 `.inp/.frd/.dat/.sta/.cvg`。没有哈希过的 ccx 产物之前，不要改「本研究」列。
